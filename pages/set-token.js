// pages/set-token.js
export default function SetTokenOk() {
  // Minimal valid React component so Next.js stops complaining.
  // (We’ll handle token logic later; goal here is to unblock the build.)
  return null;
}
